# ================================
# 🥇 MOST USED & ESSENTIAL COMMANDS
# ================================

# Start all services in background (Docker Compose)
docker-compose up -d

# Stop all services
docker-compose down

# Rebuild and restart all services
docker-compose up --build -d

# Show logs of all services
docker-compose logs

# Check running containers
docker ps

# Build services (initial or after Dockerfile changes)
docker-compose build

# Open shell inside a running service container
docker-compose exec <service_name> sh

# Run a test container (e.g. Ubuntu)
docker run -it ubuntu bash

# ================================
# 🥈 USEFUL FOR DEBUGGING OR SETUP
# ================================

# Check all containers (including stopped)
docker ps -a

# Start a stopped container
docker start <container_id>

# Stop a running container
docker stop <container_id>

# Remove a stopped container
docker rm <container_id>

# View logs from a container
docker logs <container_id>

# List local Docker images
docker images

# Download an image from Docker Hub
docker pull <image_name>

# Build an image from a Dockerfile manually
docker build -t myimage .

# Inspect Docker network
docker network inspect <network_name>

# Inspect Docker volume
docker volume inspect <volume_name>

# ================================
# 🥉 CLEANUP & MAINTENANCE
# ================================

# Remove all stopped containers
docker container prune

# Remove unused volumes
docker volume prune

# Remove dangling/unused images
docker image prune

# Remove all unused data (containers, images, volumes, networks)
docker system prune -a

# Remove all images (use with caution!)
docker rmi $(docker images -q)

# Force remove all images (including those in use)
docker rmi -f $(docker images -q)

# ================================
# 🧰 ADVANCED / LESS FREQUENT 